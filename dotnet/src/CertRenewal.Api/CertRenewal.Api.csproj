<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>CertRenewal.Api</RootNamespace>
    <Description>ASP.NET Core minimal-API endpoints for the renewal feature, plus a BackgroundService for the sweep. Framework coupling is deliberately thin.</Description>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="../CertRenewal.Core/CertRenewal.Core.csproj" />
  </ItemGroup>

  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

</Project>
